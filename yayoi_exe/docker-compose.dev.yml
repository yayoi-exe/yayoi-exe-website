services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: yayoi-exe-website-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
    stdin_open: true
    tty: true
